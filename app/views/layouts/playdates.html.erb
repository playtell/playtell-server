<!DOCTYPE html>
<html>
<head>
	<title>PlayTell - Make memories, not small talk.</title>
	<link rel="shortcut icon" href="/favicon.ico" />

	<% if tablet_device? %>
		<meta name="viewport" content="user-scalable=no, width=device-width, initial-scale=1.0, maximum-scale=1.0" />
	<% end %>

	<link href='http://fonts.googleapis.com/css?family=Lato:100,400|Comfortaa:400,300|Nobile:400,700' rel='stylesheet' type='text/css'>	
	<%= stylesheet_link_tag "scaffold.css", "aristo.css", "misc.css" %>

	<%= javascript_include_tag "https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" %>
	<%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.9/jquery-ui.min.js" %>
	<%= javascript_include_tag 'http://core-framework.googlecode.com/svn/trunk/core.js' %>
	<%= javascript_include_tag :defaults %>
	<%= javascript_include_tag "http://js.pusher.com/1.11/pusher.min.js" %>
	<%= javascript_include_tag 'pusher', 'playdate', 'jquery.lightbox_me.js', 'jquery.easing.1.3', 'activity/PTActivity', 'activity/PTBook' %>

	<%= csrf_meta_tag %>

	<script type="text/javascript">
		var tablet = false;
		var inPlaydate = false;
		var pusher;
		var playdateChannel;

		$(document).ready(function() {
			<% if tablet_device? %>
				tablet = true;
				document.body.addEventListener('touchmove', function(e){ e.preventDefault(); });
			<% end %>
			pusher = new Pusher($('#pusher-key').html()); 	
		});

	</script>

	<%= yield :head %>

</head>

<body>
	<% if !tablet_device? %>
		<p class="block fr"><%= render 'devise/menu/login_items' %></p>
		<p class="notice"><%= notice %></p>
		<p class="alert"><%= alert %></p>
	<% end %>

	<%= yield %>
	
	<%= render 'application/values' %>

</body>
</html>

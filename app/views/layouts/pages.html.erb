<!DOCTYPE html>
<html>
<head>
	<title>Page</title>
	<style type="text/css" media="screen">
		body {margin:0;}
		
		div.page {
			width:800px;
			height:600px;
			overflow:hidden;
		}
		
		div.page table.image {
			margin:0;
			padding:0;
			border-collapse:collapse;
			border-spacing:0;
		}
		
		div.page table.image td {
			width:800px;
			height:600px;
			vertical-align:middle;
			text-align:center;
		}
	</style>
</head>
<body>
<%= yield %>
<script type="text/javascript" charset="utf-8">
	var readyCheck = setInterval(function () {
		if (document.readyState === "complete") {
			document.location.href = 'playtell:pageLoadFinished:<%= @book.id.to_s %>:<%= @page.page_num.to_s %>';
			clearInterval(readyCheck);
		}
	}, 10);
</script>
</body>
</html>
//removeKeepsakes();
hideToyBox();
$('#player-container').hide();
$('#keepsake-container').hide();
$('.game-container').hide();

$('#total-pages').html("<%= @book.pages.count %>");
$('#page-num').html("<%= @playdate.page_num %>");
$('.book-nav').show();
enableNavButtons("book", "<%= Playdate::TURN_PAGE %>");
$("#book").html("<%= escape_javascript(render(:partial => 'book', :locals => {:book => @book})).html_safe %>");
showBook("<%= @book.title %>", "<%= @playdate.page_num %>", "<%= @book.pages.count %>");
$('.book-container').show();

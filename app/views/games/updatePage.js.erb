//TODO should move this to happen before the server hit to make the page more responsive from a user perspective
removeKeepsakes();
hideToyBox();
$('#player-container').hide();
$('.slide-container').hide();
$('.book-nav').hide();
$('.book-container').show();
$("#book").html("<%= escape_javascript(render(:partial => 'book', :locals => {:book => @book})).html_safe %>");
showBook("<%= @book.title %>", "<%= @playdate.page_num %>", "<%= @book.pages.count %>");
if (session) {
	session.signal();
}
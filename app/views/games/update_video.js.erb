$('.back-cover').hide();
hideToyBox();
if (keepsakeCameraOn) {
	toggleCameras("my-camera", PUBLISHER_WIDTH, PUBLISHER_HEIGHT, "");
}
$('.book-container').hide();
$('#player-container').show();
createPlayer();
if (session) {
	session.signal();
}
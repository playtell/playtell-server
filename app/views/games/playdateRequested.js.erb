<% unless @playdate.blank? %>
	$('#join-lightbox').append("<%=raw escape_javascript(render(:partial => "playdateRequest")) %>");
	$('#join-lightbox').lightbox_me({
	    centered: true, 
		destroyOnClose: true,
	});
	clearTimeout(t);
	p = $('#playdate_id').val();
	var t = setTimeout("checkForDisconnect(p)", 1000);
<% end %>